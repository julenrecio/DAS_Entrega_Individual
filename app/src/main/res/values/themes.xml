<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EntregaIndividual" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.EntregaIndividual" parent="Base.Theme.EntregaIndividual" />

    <style name="estilo1" parent="ThemeOverlay.Material3.Button.TextButton">
        <item name="android:textColor">@color/red</item>
        <item name="android:textSize">20sp</item>
        <item name="android:fontFamily">bold</item>
    </style>

    <style name="estilo2" parent="ThemeOverlay.Material3.Button.TextButton">
        <item name="android:fontFamily">bold</item>
        <item name="android:textColor">@color/yellow</item>
    </style>

    <style name="estilo3" parent="Theme.AppCompat.DayNight">
        <item name="android:fontFamily">monospace</item>
        <item name="android:textColor">@color/purple</item>
    </style>
</resources>